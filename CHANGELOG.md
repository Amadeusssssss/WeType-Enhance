# 更新日志 (Changelog)

所有针对 WeType-Enhance（微信输入法 18 键增强模块）的重要变更均记录于此。

---

## [v1.28.4-18key.2] - 2026-09-21

### 🐛 重点问题修复 (Bug Fixes)
- **修复 LSPosed 模式（原版输入法 + 模块）下按键下滑手势完全失效**
  - **现象**：在 LSPatch 免 Root 修补版中按键下滑一切正常，但在 LSPosed 模块模式下搭配原版输入法时，下滑手势完全无法触发。
  - **根因分析**：按键手势系统依赖 DexKit 动态扫描输入法底层的混淆触摸分发逻辑（`onTouch move2` 与 `(selfdraw.j, MotionEvent, selfdraw.p)Z`）。在原版输入法 + 独立模块环境下，由于 Android 7.0+ Linker ClassLoader Namespace 隔离，宿主无法跨应用加载模块 APK 内的 `.so`；同时在 LibXposed 模块早期初始化阶段，宿主尚未创建 `Application` 实例，导致原生库提取路径回退至 `/data/local/tmp` 触发 SELinux / EACCES 权限拒绝，DexKit 初始化失败并导致手势 Hook 被静默跳过。
  - **修复实现**：
    1. 在 `MainHook.onPackageReady` 中捕获并注册宿主真实数据目录（`param.applicationInfo.dataDir`），并通过 `ActiveTarget` 在热重载状态中完整持久化保存。
    2. 重构 `DexKitLoader`：引入严格的 32/64 位进程 ABI 架构匹配校验（避免 ELFCLASS 冲突）。
    3. 建立宿主专属可执行写入目录（`code_cache`、`cache`、`files`）多级动态解析与逐级加载容灾机制，彻底解决 LSPosed 环境下原版微信输入法的 DexKit 加载与手势拦截问题。
    4. 同步更新单元测试契约，覆盖全量 26 项手势动作定义。

---

## [v1.28.4-18key.1] - 2026-09-21

### ✨ 新增功能 (Features)
- **空格键上滑切换中英文**
  - 在【按键手势设置】中新增“空格上滑切换中英文”独立开关。
  - 无论通用按键手势是否全局开启，该功能均可独立生效，上滑空格快速在中文与英文键盘间流畅切换。
- **英文模式关闭候选词 / 纯净直出**
  - 在模块设置中新增“英文模式关闭候选词”开关。
  - 深度介入输入法预测与候选处理流水线，在英文模式下彻底屏蔽候选栏词汇、自动联想、自动纠错与补全推荐，实现纯净直接上屏的打字体验。
- **候选栏 / 工具栏高度调节**
  - 在模块设置中新增候选栏/工具栏高度调节滑块（支持 0 ~ 120 dp 范围自定义）。
  - 当官方设置中选择“拼音显示在输入框”时，可大幅缩减候选栏占用面积，让键盘更加紧凑清爽；界面同时提供“恢复候选栏默认高度”一键重置功能。
- **双拼展开显示完整拼音**
  - 在模块设置中新增“双拼展开完整拼音”开关。
  - 输入双拼编码时（如小鹤双拼输入 `nihc`），拼音预览区将实时展开还原为完整全拼（如 `ni'hao`），双拼音节分隔更加直观清晰。

### 🐛 问题修复 (Bug Fixes)
- **修复关闭美化开关时系统抬高底栏透明漏底**
  - 修复关闭美化总开关时，键盘最下方系统抬高工具栏变为纯透明导致透视下层应用界面的异常问题，确保底栏颜色与键盘底色保底统一。

---

## [v1.28.4-18key] - 2026-09-20

### ✨ 初始特性 (Initial Release)
- **18 键经典键盘布局支持**
  - 深度适配微信输入法，移植经典 18 键布局。
- **自定义视觉与毛玻璃材质**
  - 支持按键配色、背景、图标以及 HyperOS 材质美化与毛玻璃样式。
- **CI/CD 自动化全链路**
  - 支持 GitHub Actions 自动化编译模块、LSPatch 免 Root 独立整合包注入与腾讯 CDN 官方版本更新自动巡检。

